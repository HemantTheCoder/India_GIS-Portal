name: Keep Alive

on:
  schedule:
    - cron: "*/15 * * * *"  # Run every 15 minutes
  workflow_dispatch:        # Allow manual triggering

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App URL
        run: |
          # Replace the URL below with your actual deployed Streamlit app URL
          curl -I https://india-gis.streamlit.app/
